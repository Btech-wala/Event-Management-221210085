import React from "react";

const VendorNavbar = () => (
  <nav className="navbar navbar-dark bg-success px-3">
    <span className="navbar-brand fw-bold">Vendor Dashboard</span>
    <button className="btn btn-light btn-sm">Logout</button>
  </nav>
);

export default VendorNavbar;
